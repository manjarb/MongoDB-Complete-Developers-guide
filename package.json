{
  "name": "complete-developers-guide-mongodb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node -r ts-node/register -r tsconfig-paths/register dist/config/application.js",
    "start:prod": "npm run build && NODE_ENV=production npm run start",
    "dev": "ts-node-dev --respawn -r tsconfig-paths/register config/application.ts",
    "build": "rm -rf ./dist && tsc -p .",
    "test-dev": "NODE_ENV=test nodemon --watch . --ext ts --exec \"mocha -r ts-node/register -r tsconfig-paths/register app/**/*.spec.ts\"",
    "clear:upload": "rm -rf ./public/upload"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^9.0.0",
    "ts-node-dev": "^1.0.0-pre.62",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.10.3"
  }
}
